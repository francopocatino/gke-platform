apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: hello-api
spec:
  selector:
    matchLabels:
      app: hello-api
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
